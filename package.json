{
  "name": "my-fullstack-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@types/express-session": "^1.18.0",
    "cookie-session": "^2.1.0",
    "turbo": "^1.11.2"
  },
  "devDependencies": {
    "@types/cookie-session": "^2.0.48"
  }
}
